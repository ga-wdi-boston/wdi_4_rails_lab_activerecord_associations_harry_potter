<h2><%= @house.name %></h2>
<p>
  <strong>Students:</strong></br>
  <% @house.students.each do |student| %>
  <%= link_to student.first_name + " " + student.last_name, student_path(student.id) %></br>
  <% end %>
</p>