<%= form_for(@professor_skill) do |f| %>
  <% if @professor_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professor_skill.errors.count, "error") %> prohibited this professor_skill from being saved:</h2>

      <ul>
      <% @professor_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= f.label :spell %><br>
  <%= collection_select(:professor_skill, :spell_id, Spell.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.number_field :level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
