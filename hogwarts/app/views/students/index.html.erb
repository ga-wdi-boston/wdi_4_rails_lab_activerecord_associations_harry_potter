<h1>List All the Students!!!</h1>

<% @students.each do |student| %>
  <div class="link"><%= link_to student.name, student %> 
  <% if student.house %>
    lives in <%= link_to student.house.name, student.house %>
  <% end %>
  </div>
<% end %>