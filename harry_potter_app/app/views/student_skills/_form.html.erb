<%= form_for(@student_skill) do |f| %>
  <% if @student_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_skill.errors.count, "error") %> prohibited this student_skill from being saved:</h2>

      <ul>
      <% @student_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= f.label :spell %><br>
  <%= collection_select(:student_skill, :spell_id, Spell.all, :id, :name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.number_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
